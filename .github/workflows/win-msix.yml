name: Windows MSIX Build

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: "8.x"

    - name: Restore
      run: dotnet restore

    # Publish and capture MSBuild binary log
    - name: Publish and capture binlog
      run: dotnet publish -c Release -r win-x64 /bl

    # Upload the msbuild.binlog regardless of success/failure
    - name: Upload build log
      uses: actions/upload-artifact@v4
      with:
        name: msbuild-binlog
        path: msbuild.binlog

    # Try to package MSIX (will be skipped if publish failed)
    - name: Pack MSIX
      if: success()    # only runs if Publish succeeded
      shell: pwsh
      run: |
        choco install --yes msix-packaging
        $publish = Get-ChildItem -Recurse -Filter *.exe | Where-Object FullName -match "publish" | Select-Object -First 1
        msix pack /d $publish.Directory.FullName /p $Env:GITHUB_WORKSPACE\CryptoAccessible-M0.msix /v /f

    - name: Upload installer
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: CryptoAccessible-M0-installer
        path: CryptoAccessible-M0.msix
